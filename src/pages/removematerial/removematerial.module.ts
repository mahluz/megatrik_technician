import { NgModule } from '@angular/core';
import { IonicPageModule } from 'ionic-angular';
import { RemovematerialPage } from './removematerial';

@NgModule({
  declarations: [
    RemovematerialPage,
  ],
  imports: [
    IonicPageModule.forChild(RemovematerialPage),
  ],
})
export class RemovematerialPageModule {}
